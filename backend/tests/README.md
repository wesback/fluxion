# Tests

This directory will contain tests for the Fluxion application.

## Structure

- Unit tests for models
- Integration tests for database operations
- Tests for API endpoints (when implemented)

## Running Tests

```bash
pytest
```

Or with coverage:

```bash
pytest --cov=fluxion --cov-report=html
```
